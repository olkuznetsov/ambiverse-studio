# Screenshots

Drop three PNGs here and the Screenshots table in the top-level README will show them
(uncomment that block once the files exist):

- `dashboard.png` — the Dashboard: per-theme readiness cards, music/Veo banks, token & disk status.
- `channel.png` — the Channel tab: YPP progress bars, overview cards, growth charts, per-world retention rollup.
- `build.png` — the Build wizard: main-build form with the live command preview and inline preflight panel.

Capture them from the running app at `http://localhost:5175` (a ~1280px-wide window looks best).
